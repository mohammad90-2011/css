# css
Tabchi


sudo apt-get update; sudo apt-get upgrade
git clone https://github.com/mohammad90-2011/css.git && cd css && chmod 777 torinstall.sh && ./torinstall.sh
با دستور زیر سرور رو ری استارت میکنیم
reboot
دوباره صفحه ترمینال رو باز کرده و دستورات زیر رو به ترتیب وارد می کنیم
cd css && chmod +x bot && ./bot install
./bot create
./bot 1
آی دی عددی ادمین ربات رو وارد کنید
شماره ربات را وارد کنید
کد دریافت شده در تلگرام رو وارد کنید.
تمام!!!! ربات در حال کار هست

برای این که چند ربات بسازیم کافیه برای ساخت هر ربات دستورات زیر را یکبار اجرا کنیم
cd css
./bot create
./bot 1
آی دی عددی ادمین ربات رو وارد کنید
شماره ربات را وارد کنید
کد دریافت شده در تلگرام رو وارد کنید.

پس از ساختن تمام ربات ها اتولانچ رو فعال میکنیم با دستور زیر تا ربات ها آنلاین بمونن
cd css && ./bot autolaunch
دیگه ربات ها آفلاین نمیشن
